# Test login with Auth0

### Install modules
Run command `yarn` or `npm install` in the project directory.

### Run project

#### Run Android
Connect a device or run emulator and run command `react-native run-android`.

#### Run iOS
Run command `react-native run-ios` to start simulator.